services:
  foops:
    build: .
    image: foops:latest
    container_name: foops
    ports:
      # host:container -> 8083 matches the examples in the README
      - "8083:8080"
    environment:
      - SERVER_PORT=8080
      # - JAVA_OPTS=-Xmx1g
    restart: unless-stopped
