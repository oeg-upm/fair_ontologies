package entities.checks;

import entities.Check;
import entities.Ontology;

public class CheckPrefixInRegistry extends Check {
    public CheckPrefixInRegistry(Ontology o) {
        super(o);
    }
}
