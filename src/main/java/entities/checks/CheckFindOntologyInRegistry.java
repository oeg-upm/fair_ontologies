package entities.checks;

import entities.Check;
import entities.Ontology;

public class CheckFindOntologyInRegistry extends Check {
    public CheckFindOntologyInRegistry(Ontology o) {
        super(o);
    }
}
